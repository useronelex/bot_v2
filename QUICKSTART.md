# 🚀 ШВИДКИЙ СТАРТ - Instagram Telegram Bot

## ✅ Що змінено

1. **Замінено polling на webhook** - тепер бот працює через HTTP запити замість постійного опитування
2. **Об'єднано Flask та bot** - все в одному процесі
3. **Видалено keep_alive.py** - більше не потрібен окремий Flask сервер

## 📦 Нові файли

- `main.py` - Flask сервер з webhook endpoints
- `bot.py` - логіка бота (без polling)
- `requirements.txt` - без змін
- `README.md` - детальні інструкції
- `.env.example` - шаблон для змінних
- `.gitignore` - що не комітити в Git

## ⚡ Як задеплоїти (за 5 хвилин)

### 1. Завантажте нові файли
Замініть старі файли на нові з цієї папки

### 2. Налаштування на Render.com

**Build Command:**
```
pip install -r requirements.txt
```

**Start Command:**
```
python main.py
```

**Environment Variables:**
```
BOT_TOKEN=123456:ABC-DEF1234ghIkl...
WEBHOOK_URL=https://your-app.onrender.com
```

### 3. Після першого деплою

1. Скопіюйте URL вашого додатку (наприклад: `https://instagram-bot-abc123.onrender.com`)
2. Додайте його в Environment Variables як `WEBHOOK_URL`
3. Зробіть Redeploy
4. Відкрийте в браузері: `https://your-app.onrender.com/set_webhook`
5. Готово! ✅

### 4. Перевірка

```
https://your-app.onrender.com/            # Перевірка чи працює
https://your-app.onrender.com/webhook_info # Статус webhook
```

## 🆘 Якщо щось не працює

1. Перевірте логи на Render
2. Переконайтесь що обидві змінні (`BOT_TOKEN` і `WEBHOOK_URL`) встановлені
3. Перевстановіть webhook через `/set_webhook`

## 📞 Endpoints

- `/` - статус бота
- `/health` - health check
- `/set_webhook` - встановити webhook ⭐
- `/webhook_info` - інфо про webhook
- `/delete_webhook` - видалити webhook

---

**Важливо:** Не забудьте налаштувати UptimeRobot (https://uptimerobot.com) 
щоб бот не засинав на безкоштовному плані Render!
